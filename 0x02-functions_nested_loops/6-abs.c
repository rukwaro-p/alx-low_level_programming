#include "main.h"

/**
 * _abs - Finds absolute value of an integer
 *@n: the character to be checked
 * Return: the abs value of the given int
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
